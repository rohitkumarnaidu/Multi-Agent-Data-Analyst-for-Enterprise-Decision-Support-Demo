# Docker Requirements for Python 3.12 Environment
# Bypasses the tiktoken/regex compilation issues present on Python 3.14 on Windows.

pandas>=2.0.0
duckdb>=0.10.0
pyarrow>=15.0.0
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0

# Newer crewai version supported natively in Python 3.12 without compilation errors!
crewai>=0.28.8
langchain-community
langchain-openai

matplotlib>=3.8.0
python-dotenv>=1.0.0
loguru>=0.7.0
rich>=13.7.0

# UI & API
fastapi
uvicorn
streamlit
